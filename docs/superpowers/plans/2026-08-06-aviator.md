# Aviator (argent fictif) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** PWA vanilla « Aviator » (crash game) à argent fictif : salon avec carrousel d'avions, écran de jeu animé sur canvas, onglet stats avec courbe d'évolution du capital.

**Architecture:** Une seule page (`index.html`) avec 3 sections affichées/cachées (salon / jeu / stats). `game.js` contient la logique pure (tirage du crash, mises, gains, persistance) testée sous Node ; `app.js` contient l'UI, la machine à états (ATTENTE → VOL → CRASH) et les rendus canvas. Données en localStorage, clé `aviator-data`.

**Tech Stack:** HTML/CSS/JS vanilla, canvas 2D, Node (tests uniquement), Swift/Vision (détourage des images, outil ponctuel), GitHub Pages.

## Global Constraints

- Aucune dépendance externe au runtime (pas de CDN, pas de librairie).
- Argent 100 % fictif, aucun compte, aucune connexion réseau en jeu.
- Textes UI en français.
- Mobile-first (iPhone Safari), installable en PWA.
- Jetons = entiers (pas de centimes). Solde de départ : 1000. Mise minimum : 10.
- Repo : `/Users/elliot/aviator`, déployé sur `https://ewzix1234.github.io/aviator/`.
- localStorage clé unique : `aviator-data`.
- Commits fréquents, messages en français, suffixe `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

---

### Task 1: Logique pure `game.js` + tests Node

**Files:**
- Create: `game.js`
- Test: `tests/game.test.js`

**Interfaces:**
- Produces (utilisés par app.js, exposés sur `window.Game` en navigateur et `module.exports` sous Node) :
  - `AVIONS: [{id, nom, img}]`
  - `DONNEES_DEFAUT: {solde, avion, capitalHistorique, crashHistorique, stats}`
  - `tirerCrash(rnd?: () => number): number` — multiplicateur de crash ≥ 1.00, plafonné à 1000, 2 décimales
  - `chargerDonnees(storage): donnees` — lit/répare `aviator-data`
  - `sauverDonnees(storage, donnees): void`
  - `placerMise(donnees, montant): {ok: true} | {ok: false, erreur: string}` — déduit le solde, incrémente `stats.totalMise`
  - `resoudreManche(donnees, {mise, crash, encaisseA}): {gain: number}` — `encaisseA` = multiplicateur encaissé ou `null` (perdu) ; crédite le gain, met à jour stats et `capitalHistorique`
  - `enregistrerCrash(donnees, crash): void` — pousse dans `crashHistorique` (max 20 entrées)

- [ ] **Step 1: Écrire les tests qui échouent**

```js
// tests/game.test.js
const assert = require('assert');
const G = require('../game.js');

// --- tirerCrash ---
assert.strictEqual(G.tirerCrash(() => 0), 1.00, 'r=0 -> x1.00');
assert.strictEqual(G.tirerCrash(() => 0.505), 2.00, 'r=0.505 -> x2.00');
assert.strictEqual(G.tirerCrash(() => 0.9999999), 1000, 'plafond x1000');
// médiane ~x2 : sur 10000 tirages, entre 40% et 60% sous x2
let sous2 = 0;
for (let i = 0; i < 10000; i++) if (G.tirerCrash() < 2) sous2++;
assert.ok(sous2 > 4000 && sous2 < 6000, 'mediane ~x2, obtenu ' + sous2);

// --- chargerDonnees ---
const mem = (init) => ({ d: init, getItem(k){ return this.d; }, setItem(k, v){ this.d = v; } });
let d = G.chargerDonnees(mem(null));
assert.strictEqual(d.solde, 1000, 'solde par defaut');
assert.strictEqual(d.avion, 'lufthansa', 'avion par defaut');
d = G.chargerDonnees(mem('{corrompu'));
assert.strictEqual(d.solde, 1000, 'json corrompu -> defaut');
d = G.chargerDonnees(mem('{"solde":250,"avion":"emirates"}'));
assert.strictEqual(d.solde, 250, 'solde conserve');
assert.deepStrictEqual(d.capitalHistorique, [], 'champs manquants completes');

// --- sauverDonnees ---
const st = mem(null);
G.sauverDonnees(st, d);
assert.strictEqual(JSON.parse(st.d).solde, 250, 'sauvegarde ecrite');

// --- placerMise ---
d = G.chargerDonnees(mem(null));
let r = G.placerMise(d, 100);
assert.strictEqual(r.ok, true);
assert.strictEqual(d.solde, 900, 'mise deduite');
assert.strictEqual(d.stats.totalMise, 100);
r = G.placerMise(d, 5000);
assert.strictEqual(r.ok, false, 'solde insuffisant refuse');
r = G.placerMise(d, 5);
assert.strictEqual(r.ok, false, 'mise < 10 refusee');
r = G.placerMise(d, 10.5);
assert.strictEqual(r.ok, false, 'mise non entiere refusee');

// --- resoudreManche : gain ---
d = G.chargerDonnees(mem(null));
G.placerMise(d, 100); // solde 900
let res = G.resoudreManche(d, { mise: 100, crash: 5.00, encaisseA: 2.5 });
assert.strictEqual(res.gain, 250, 'gain = mise x mult arrondi');
assert.strictEqual(d.solde, 1150);
assert.strictEqual(d.stats.totalGagne, 250);
assert.strictEqual(d.stats.plusGrosGain, 250);
assert.strictEqual(d.stats.nbParties, 1);
assert.deepStrictEqual(d.capitalHistorique, [1150], 'capital enregistre');

// --- resoudreManche : perte ---
G.placerMise(d, 150); // solde 1000
res = G.resoudreManche(d, { mise: 150, crash: 1.24, encaisseA: null });
assert.strictEqual(res.gain, 0);
assert.strictEqual(d.solde, 1000);
assert.strictEqual(d.stats.nbParties, 2);
assert.deepStrictEqual(d.capitalHistorique, [1150, 1000]);

// --- enregistrerCrash ---
for (let i = 0; i < 25; i++) G.enregistrerCrash(d, 1.5 + i);
assert.strictEqual(d.crashHistorique.length, 20, 'historique crash plafonne a 20');
assert.strictEqual(d.crashHistorique[d.crashHistorique.length - 1], 25.5, 'plus recent en dernier');

// --- AVIONS ---
assert.ok(Array.isArray(G.AVIONS) && G.AVIONS.length >= 2, 'au moins 2 avions');
assert.ok(G.AVIONS.every(a => a.id && a.nom && a.img), 'champs avion complets');

console.log('OK — tous les tests game.js passent');
```

- [ ] **Step 2: Vérifier que les tests échouent**

Run: `cd /Users/elliot/aviator && node tests/game.test.js`
Expected: FAIL (`Cannot find module '../game.js'`)

- [ ] **Step 3: Implémenter `game.js`**

```js
// game.js — logique pure du jeu (aucun DOM). Testable sous Node.
(function (racine) {
  'use strict';

  const AVIONS = [
    { id: 'lufthansa', nom: 'Lufthansa A380', img: 'img/lufthansa.png' },
    { id: 'emirates',  nom: 'Emirates A380',  img: 'img/emirates.png' },
  ];

  const MISE_MIN = 10;
  const SOLDE_DEPART = 1000;
  const MAX_CRASHS = 20;

  const DONNEES_DEFAUT = Object.freeze({
    solde: SOLDE_DEPART,
    avion: 'lufthansa',
    capitalHistorique: [],
    crashHistorique: [],
    stats: { totalMise: 0, totalGagne: 0, plusGrosGain: 0, nbParties: 0 },
  });

  function neufDonnees() {
    return JSON.parse(JSON.stringify(DONNEES_DEFAUT));
  }

  function tirerCrash(rnd) {
    const r = (rnd || Math.random)();
    const brut = 0.99 / (1 - r);
    const crash = Math.max(1.00, Math.min(1000, brut));
    return Math.floor(crash * 100) / 100;
  }

  function chargerDonnees(storage) {
    const defaut = neufDonnees();
    let lu;
    try {
      lu = JSON.parse(storage.getItem('aviator-data'));
    } catch (e) {
      lu = null;
    }
    if (!lu || typeof lu !== 'object') return defaut;
    const d = defaut;
    if (Number.isFinite(lu.solde) && lu.solde >= 0) d.solde = Math.floor(lu.solde);
    if (AVIONS.some(a => a.id === lu.avion)) d.avion = lu.avion;
    if (Array.isArray(lu.capitalHistorique)) d.capitalHistorique = lu.capitalHistorique.filter(Number.isFinite);
    if (Array.isArray(lu.crashHistorique)) d.crashHistorique = lu.crashHistorique.filter(Number.isFinite).slice(-MAX_CRASHS);
    if (lu.stats && typeof lu.stats === 'object') {
      for (const k of ['totalMise', 'totalGagne', 'plusGrosGain', 'nbParties']) {
        if (Number.isFinite(lu.stats[k]) && lu.stats[k] >= 0) d.stats[k] = lu.stats[k];
      }
    }
    return d;
  }

  function sauverDonnees(storage, donnees) {
    storage.setItem('aviator-data', JSON.stringify(donnees));
  }

  function placerMise(donnees, montant) {
    if (!Number.isInteger(montant)) return { ok: false, erreur: 'La mise doit être un nombre entier.' };
    if (montant < MISE_MIN) return { ok: false, erreur: 'Mise minimum : ' + MISE_MIN + ' jetons.' };
    if (montant > donnees.solde) return { ok: false, erreur: 'Solde insuffisant.' };
    donnees.solde -= montant;
    donnees.stats.totalMise += montant;
    return { ok: true };
  }

  function resoudreManche(donnees, manche) {
    let gain = 0;
    if (manche.encaisseA !== null && manche.encaisseA !== undefined) {
      gain = Math.round(manche.mise * manche.encaisseA);
      donnees.solde += gain;
      donnees.stats.totalGagne += gain;
      if (gain > donnees.stats.plusGrosGain) donnees.stats.plusGrosGain = gain;
    }
    donnees.stats.nbParties += 1;
    donnees.capitalHistorique.push(donnees.solde);
    return { gain };
  }

  function enregistrerCrash(donnees, crash) {
    donnees.crashHistorique.push(crash);
    if (donnees.crashHistorique.length > MAX_CRASHS) {
      donnees.crashHistorique.splice(0, donnees.crashHistorique.length - MAX_CRASHS);
    }
  }

  const api = {
    AVIONS, DONNEES_DEFAUT, MISE_MIN, SOLDE_DEPART,
    tirerCrash, chargerDonnees, sauverDonnees,
    placerMise, resoudreManche, enregistrerCrash,
  };

  if (typeof module !== 'undefined' && module.exports) module.exports = api;
  else racine.Game = api;
})(typeof window !== 'undefined' ? window : globalThis);
```

- [ ] **Step 4: Vérifier que les tests passent**

Run: `cd /Users/elliot/aviator && node tests/game.test.js`
Expected: `OK — tous les tests game.js passent`

- [ ] **Step 5: Commit**

```bash
cd /Users/elliot/aviator && git add game.js tests/game.test.js && git commit -m "feat: logique pure du jeu (tirage crash, mises, gains, persistance)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: Détourage des avions + images du projet

**Files:**
- Create: `tools/detourer.swift`
- Create: `img/lufthansa.png`, `img/emirates.png` (avions détourés, fond transparent)
- Create: `img/icon-192.png`, `img/icon-512.png` (icônes PWA)

**Interfaces:**
- Consumes: sources `/Users/elliot/Downloads/il_570xN.6504484005_e3jz.jpg.webp` (Lufthansa) et `/Users/elliot/Downloads/il_570xN.5704257651_tbqi.jpg.avif` (Emirates). Les copies `-2`/`-3` sont des doublons identiques (même md5), on les ignore.
- Produces: PNG transparents `img/lufthansa.png` et `img/emirates.png` référencés par `Game.AVIONS` (Task 1) ; icônes référencées par `manifest.json` (Task 6).

- [ ] **Step 1: Écrire l'outil de détourage (Vision, macOS 14+)**

```swift
// tools/detourer.swift — usage: swift tools/detourer.swift entree.png sortie.png
import Foundation
import Vision
import CoreImage
import AppKit

let args = CommandLine.arguments
guard args.count == 3 else { fputs("usage: detourer.swift entree sortie\n", stderr); exit(1) }
let entree = URL(fileURLWithPath: args[1])
let sortie = URL(fileURLWithPath: args[2])

guard let ciImage = CIImage(contentsOf: entree) else { fputs("lecture impossible\n", stderr); exit(1) }
let requete = VNGenerateForegroundInstanceMaskRequest()
let gestionnaire = VNImageRequestHandler(ciImage: ciImage)
try gestionnaire.perform([requete])
guard let resultat = requete.results?.first else { fputs("aucun sujet detecte\n", stderr); exit(1) }
let masque = try resultat.generateScaledMaskForImage(forInstances: resultat.allInstances, from: gestionnaire)

let filtre = CIFilter(name: "CIBlendWithMask")!
filtre.setValue(ciImage, forKey: kCIInputImageKey)
filtre.setValue(CIImage(color: .clear).cropped(to: ciImage.extent), forKey: kCIInputBackgroundImageKey)
filtre.setValue(CIImage(cvPixelBuffer: masque), forKey: kCIInputMaskImageKey)

let contexte = CIContext()
guard let cg = contexte.createCGImage(filtre.outputImage!, from: ciImage.extent) else { exit(1) }
let rep = NSBitmapImageRep(cgImage: cg)
try rep.representation(using: .png, properties: [:])!.write(to: sortie)
print("ecrit:", sortie.path)
```

- [ ] **Step 2: Convertir les sources et lancer le détourage**

```bash
cd /Users/elliot/aviator
S=/private/tmp/claude-501/-Users-elliot/54b34164-68b5-4aeb-934a-aca937debb46/scratchpad
sips -s format png "/Users/elliot/Downloads/il_570xN.6504484005_e3jz.jpg.webp" --out "$S/lufthansa-src.png"
sips -s format png "/Users/elliot/Downloads/il_570xN.5704257651_tbqi.jpg.avif" --out "$S/emirates-src.png"
swift tools/detourer.swift "$S/lufthansa-src.png" img/lufthansa.png
swift tools/detourer.swift "$S/emirates-src.png" img/emirates.png
```

Expected: `ecrit: .../img/lufthansa.png` et `ecrit: .../img/emirates.png`

- [ ] **Step 3: Recadrer sur le contenu utile et vérifier visuellement**

```bash
cd /Users/elliot/aviator
# rogner les bords transparents (le sticker occupe ~le tiers central de l'image 570x570)
node -e "console.log('verification manuelle: ouvrir les PNG')"
```

Ouvrir `img/lufthansa.png` et `img/emirates.png` avec le tool Read (multimodal) et vérifier :
fond transparent, avion entier (nez, queue, réacteurs), pas de morceaux de bokeh restants.
Si le masque a raté (avion tronqué), fallback : garder le sticker entier (bordure blanche
comprise) en rognant manuellement un rectangle autour du sticker avec `sips -c`.

- [ ] **Step 4: Générer les icônes PWA**

```bash
cd /Users/elliot/aviator
sips -Z 400 img/lufthansa.png --out /tmp/icone-brute.png 2>/dev/null || cp img/lufthansa.png /tmp/icone-brute.png
sips -Z 400 img/lufthansa.png --padToHeightWidth 512 512 --padColor 16213E --out img/icon-512.png
sips -Z 150 img/lufthansa.png --padToHeightWidth 192 192 --padColor 16213E --out img/icon-192.png
```

Expected: deux PNG carrés fond bleu nuit avec l'avion centré. Vérifier avec Read.
Note : si `--padColor` échoue sur PNG alpha, aplatir d'abord (`sips -s format jpeg` puis pad puis reconvertir).

- [ ] **Step 5: Commit**

```bash
cd /Users/elliot/aviator && git add tools/ img/ && git commit -m "feat: avions détourés (Lufthansa, Emirates) et icônes PWA

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: Squelette HTML/CSS + navigation + salon (carrousel d'avions)

**Files:**
- Create: `index.html`
- Create: `style.css`
- Create: `app.js`

**Interfaces:**
- Consumes: `window.Game` (Task 1), `img/*.png` (Task 2).
- Produces: sections `#ecran-salon`, `#ecran-jeu`, `#ecran-stats` ; fonctions `afficherEcran(nom)`, `majSolde()` ; état global `donnees` (chargé via `Game.chargerDonnees(localStorage)`) utilisés par Tasks 4 et 5. IDs DOM listés dans le HTML ci-dessous (contrat pour Tasks 4-5).

- [ ] **Step 1: Écrire `index.html`**

```html
<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <meta name="theme-color" content="#16213E">
  <title>Aviator — argent fictif</title>
  <link rel="stylesheet" href="style.css">
  <link rel="manifest" href="manifest.json">
  <link rel="apple-touch-icon" href="img/icon-192.png">
</head>
<body>
  <header class="entete">
    <button id="btn-retour-salon" class="btn-retour cache">← Salon</button>
    <div class="solde">🪙 <span id="affiche-solde">1000</span></div>
    <button id="btn-recharger" class="btn-recharger cache">Recharger</button>
  </header>

  <!-- ÉCRAN SALON -->
  <section id="ecran-salon" class="ecran">
    <h1 class="titre">AVIATOR</h1>
    <p class="sous-titre">Argent 100 % fictif — juste pour s'amuser</p>
    <div class="carrousel">
      <button id="btn-avion-prec" class="fleche" aria-label="Avion précédent">◀</button>
      <div class="avion-vitrine">
        <img id="img-avion-salon" src="img/lufthansa.png" alt="Avion sélectionné">
        <div id="nom-avion" class="nom-avion">Lufthansa A380</div>
      </div>
      <button id="btn-avion-suiv" class="fleche" aria-label="Avion suivant">▶</button>
    </div>
    <button id="btn-jouer" class="btn-principal">JOUER</button>
    <button id="btn-stats" class="btn-secondaire">📈 Mes stats</button>
  </section>

  <!-- ÉCRAN JEU -->
  <section id="ecran-jeu" class="ecran cache">
    <div id="bande-crashs" class="bande-crashs"></div>
    <div class="zone-vol">
      <canvas id="canvas-vol"></canvas>
      <div id="affiche-mult" class="mult">x1.00</div>
      <div id="message-vol" class="message-vol">Prochain vol dans <span id="compte-rebours">5</span> s</div>
    </div>
    <div class="panneau-mise">
      <div class="ligne-mise">
        <label for="champ-mise">Mise</label>
        <input id="champ-mise" type="number" inputmode="numeric" min="10" step="10" value="50">
        <div class="mises-rapides">
          <button data-mise="10">10</button><button data-mise="50">50</button>
          <button data-mise="100">100</button><button data-mise="max">MAX</button>
        </div>
      </div>
      <div class="ligne-mise">
        <label for="champ-auto">Auto-encaisser à</label>
        <input id="champ-auto" type="number" inputmode="decimal" min="1.1" step="0.1" placeholder="x (optionnel)">
      </div>
      <button id="btn-action" class="btn-principal">MISER</button>
      <div id="message-erreur" class="message-erreur"></div>
    </div>
  </section>

  <!-- ÉCRAN STATS -->
  <section id="ecran-stats" class="ecran cache">
    <h2>📈 Évolution du capital</h2>
    <div class="selecteur-duree" id="selecteur-duree">
      <button data-duree="10">10</button>
      <button data-duree="25">25</button>
      <button data-duree="50">50</button>
      <button data-duree="tout" class="actif">Tout</button>
    </div>
    <canvas id="canvas-stats"></canvas>
    <div id="resume-stats" class="resume-stats"></div>
  </section>

  <script src="game.js"></script>
  <script src="app.js"></script>
</body>
</html>
```

- [ ] **Step 2: Écrire `style.css`**

Thème sombre bleu nuit type Aviator, mobile-first. Contenu complet :

```css
* { margin: 0; padding: 0; box-sizing: border-box; }
:root {
  --fond: #1A1A2E; --panneau: #16213E; --accent: #E94560;
  --texte: #EAEAEA; --texte-faible: #9BA4B4;
  --vert: #2ECC71; --rouge: #E74C3C; --or: #F1C40F;
}
html, body { height: 100%; }
body {
  background: var(--fond); color: var(--texte);
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  display: flex; flex-direction: column;
  padding: env(safe-area-inset-top) env(safe-area-inset-right) env(safe-area-inset-bottom) env(safe-area-inset-left);
  -webkit-user-select: none; user-select: none;
}
.cache { display: none !important; }

.entete {
  display: flex; align-items: center; justify-content: space-between;
  padding: 12px 16px; background: var(--panneau);
  position: sticky; top: 0; z-index: 10;
}
.solde { font-size: 1.3rem; font-weight: 700; margin-left: auto; }
.btn-retour, .btn-recharger {
  background: none; border: 1px solid var(--texte-faible); color: var(--texte);
  border-radius: 8px; padding: 6px 12px; font-size: 0.9rem;
}
.btn-recharger { border-color: var(--vert); color: var(--vert); margin-left: 12px; }
.btn-retour:disabled { opacity: 0.35; }

.ecran { flex: 1; display: flex; flex-direction: column; align-items: center; padding: 20px 16px; gap: 18px; overflow-y: auto; }

.titre { font-size: 2.6rem; letter-spacing: 4px; color: var(--accent); margin-top: 8vh; }
.sous-titre { color: var(--texte-faible); }
.carrousel { display: flex; align-items: center; gap: 8px; width: 100%; justify-content: center; }
.fleche {
  background: var(--panneau); color: var(--texte); border: none; border-radius: 50%;
  width: 52px; height: 52px; font-size: 1.4rem; flex-shrink: 0;
}
.fleche:active { background: var(--accent); }
.avion-vitrine { text-align: center; width: min(60vw, 340px); }
.avion-vitrine img { width: 100%; height: auto; filter: drop-shadow(0 8px 16px rgba(0,0,0,0.5)); }
.nom-avion { margin-top: 10px; color: var(--texte-faible); font-weight: 600; }

.btn-principal {
  background: var(--accent); color: white; border: none; border-radius: 14px;
  font-size: 1.4rem; font-weight: 800; padding: 16px 0; width: min(80vw, 340px);
  letter-spacing: 2px; box-shadow: 0 6px 0 #B33951;
}
.btn-principal:active { transform: translateY(3px); box-shadow: 0 3px 0 #B33951; }
.btn-principal:disabled { background: #555; box-shadow: none; }
.btn-principal.mode-encaisser { background: var(--vert); box-shadow: 0 6px 0 #1E8449; }
.btn-secondaire {
  background: none; border: 2px solid var(--panneau); color: var(--texte);
  border-radius: 14px; font-size: 1.05rem; padding: 12px 0; width: min(80vw, 340px);
}

.bande-crashs { display: flex; gap: 6px; width: 100%; overflow-x: auto; padding-bottom: 4px; }
.puce-crash { border-radius: 12px; padding: 4px 10px; font-size: 0.85rem; font-weight: 700; flex-shrink: 0; background: var(--panneau); }
.puce-crash.bas { color: var(--rouge); }
.puce-crash.moyen { color: var(--vert); }
.puce-crash.haut { color: var(--or); }

.zone-vol { position: relative; width: 100%; }
#canvas-vol { width: 100%; height: 42vh; display: block; background: var(--panneau); border-radius: 14px; }
.mult {
  position: absolute; top: 12px; left: 0; right: 0; text-align: center;
  font-size: 2.6rem; font-weight: 800; pointer-events: none;
}
.mult.crash { color: var(--rouge); }
.mult.gagne { color: var(--vert); }
.message-vol {
  position: absolute; bottom: 12px; left: 0; right: 0; text-align: center;
  color: var(--texte-faible); pointer-events: none;
}

.panneau-mise { width: 100%; max-width: 420px; display: flex; flex-direction: column; gap: 12px; }
.ligne-mise { display: flex; align-items: center; gap: 10px; flex-wrap: wrap; }
.ligne-mise label { color: var(--texte-faible); font-size: 0.95rem; }
.ligne-mise input {
  background: var(--panneau); border: 1px solid #2C3E66; color: var(--texte);
  border-radius: 10px; padding: 12px; font-size: 1.1rem; width: 110px; text-align: center;
}
.mises-rapides { display: flex; gap: 6px; }
.mises-rapides button {
  background: var(--panneau); color: var(--texte); border: none;
  border-radius: 8px; padding: 10px 12px; font-size: 0.9rem;
}
.message-erreur { color: var(--rouge); text-align: center; min-height: 1.2em; font-size: 0.95rem; }

#ecran-stats h2 { margin-top: 8px; }
.selecteur-duree { display: flex; gap: 8px; }
.selecteur-duree button {
  background: var(--panneau); color: var(--texte-faible); border: none;
  border-radius: 10px; padding: 10px 18px; font-size: 1rem; font-weight: 600;
}
.selecteur-duree button.actif { background: var(--accent); color: white; }
#canvas-stats { width: 100%; max-width: 480px; height: 40vh; background: var(--panneau); border-radius: 14px; }
.resume-stats { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; width: 100%; max-width: 480px; }
.carte-stat { background: var(--panneau); border-radius: 12px; padding: 12px; text-align: center; }
.carte-stat .valeur { font-size: 1.3rem; font-weight: 800; }
.carte-stat .libelle { color: var(--texte-faible); font-size: 0.8rem; margin-top: 2px; }
```

- [ ] **Step 3: Écrire `app.js` (partie navigation + salon + solde)**

```js
// app.js — UI : navigation, salon, jeu, stats
'use strict';

const donnees = Game.chargerDonnees(localStorage);
function sauver() { Game.sauverDonnees(localStorage, donnees); }

const $ = (id) => document.getElementById(id);

// --- Solde + recharge ---
function majSolde() {
  $('affiche-solde').textContent = donnees.solde;
  $('btn-recharger').classList.toggle('cache', donnees.solde >= Game.MISE_MIN);
}
$('btn-recharger').addEventListener('click', () => {
  donnees.solde = Game.SOLDE_DEPART;
  sauver(); majSolde();
});

// --- Navigation ---
let ecranCourant = 'salon';
function afficherEcran(nom) {
  ecranCourant = nom;
  for (const e of ['salon', 'jeu', 'stats']) {
    $('ecran-' + e).classList.toggle('cache', e !== nom);
  }
  $('btn-retour-salon').classList.toggle('cache', nom === 'salon');
  if (nom === 'stats') dessinerStats();
}
$('btn-retour-salon').addEventListener('click', () => afficherEcran('salon'));
$('btn-jouer').addEventListener('click', () => afficherEcran('jeu'));
$('btn-stats').addEventListener('click', () => afficherEcran('stats'));

// --- Carrousel d'avions (salon) ---
let indexAvion = Math.max(0, Game.AVIONS.findIndex(a => a.id === donnees.avion));
const imageAvion = new Image(); // utilisée aussi par le canvas de vol

function majAvion() {
  const avion = Game.AVIONS[indexAvion];
  $('img-avion-salon').src = avion.img;
  $('nom-avion').textContent = avion.nom;
  imageAvion.src = avion.img;
  donnees.avion = avion.id;
  sauver();
}
$('btn-avion-prec').addEventListener('click', () => {
  indexAvion = (indexAvion - 1 + Game.AVIONS.length) % Game.AVIONS.length;
  majAvion();
});
$('btn-avion-suiv').addEventListener('click', () => {
  indexAvion = (indexAvion + 1) % Game.AVIONS.length;
  majAvion();
});

// Placeholders remplis par les Tasks 4 et 5 :
function dessinerStats() {} // remplacé en Task 5

majSolde();
majAvion();
afficherEcran('salon');
```

Note : `function dessinerStats() {}` est un stub volontaire remplacé en Task 5 —
en Task 3 l'écran stats s'affiche vide, c'est attendu.

- [ ] **Step 4: Test manuel navigation**

```bash
cd /Users/elliot/aviator && open index.html
```

Vérifier : solde 1000 affiché, flèches ◀ ▶ changent l'avion (image + nom), le choix
survit à un rechargement de page, JOUER affiche l'écran jeu (vide de logique),
📈 Mes stats affiche l'écran stats, ← Salon revient au salon.

- [ ] **Step 5: Commit**

```bash
cd /Users/elliot/aviator && git add index.html style.css app.js && git commit -m "feat: salon avec carrousel d'avions, navigation 3 écrans, solde persistant

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: Écran de jeu — machine à états, mises, canvas de vol

**Files:**
- Modify: `app.js` (remplacer le bloc entre `// --- Carrousel` et la fin par l'ajout du bloc jeu avant les lignes finales)

**Interfaces:**
- Consumes: `Game.tirerCrash/placerMise/resoudreManche/enregistrerCrash`, `donnees`, `sauver()`, `majSolde()`, `imageAvion`, `afficherEcran`, `ecranCourant`.
- Produces: rien pour les autres tasks (écran terminal).

- [ ] **Step 1: Ajouter le bloc jeu dans `app.js`** (avant les 3 lignes finales `majSolde(); majAvion(); afficherEcran('salon');`)

```js
// --- Écran jeu : machine à états ---
const K_CROISSANCE = 0.075;           // m(t) = e^(k*t) : x2 vers ~9 s
const DUREE_ATTENTE = 5;              // secondes
const DUREE_APRES_CRASH = 2.5;        // secondes

const etatJeu = {
  phase: 'ATTENTE',        // ATTENTE | VOL | CRASH
  finAttente: 0,           // timestamp ms
  debutVol: 0,             // timestamp ms
  crashA: 1,               // multiplicateur de crash tiré
  finCrash: 0,             // timestamp ms
  miseEnCours: 0,          // 0 = pas de mise placée
  miseProchaine: 0,        // mise placée pendant ATTENTE
  encaisseA: null,         // multiplicateur encaissé cette manche
};

const canvasVol = $('canvas-vol');
const ctxVol = canvasVol.getContext('2d');

function multCourant(maintenant) {
  const t = (maintenant - etatJeu.debutVol) / 1000;
  return Math.exp(K_CROISSANCE * t);
}

function majBandeCrashs() {
  $('bande-crashs').innerHTML = donnees.crashHistorique.slice().reverse().map(c => {
    const classe = c >= 10 ? 'haut' : c >= 2 ? 'moyen' : 'bas';
    return '<span class="puce-crash ' + classe + '">x' + c.toFixed(2) + '</span>';
  }).join('');
}

function majBoutonAction() {
  const btn = $('btn-action');
  btn.classList.toggle('mode-encaisser', etatJeu.phase === 'VOL' && etatJeu.miseEnCours > 0);
  if (etatJeu.phase === 'VOL' && etatJeu.miseEnCours > 0) {
    btn.disabled = false;
  } else if (etatJeu.phase === 'ATTENTE') {
    btn.textContent = etatJeu.miseProchaine > 0 ? 'MISE PLACÉE : ' + etatJeu.miseProchaine : 'MISER';
    btn.disabled = etatJeu.miseProchaine > 0;
  } else {
    btn.textContent = 'MISER';
    btn.disabled = true;
  }
  $('btn-retour-salon').disabled = (etatJeu.phase === 'VOL' && etatJeu.miseEnCours > 0);
}

$('btn-action').addEventListener('click', () => {
  $('message-erreur').textContent = '';
  if (etatJeu.phase === 'ATTENTE') {
    const montant = parseInt($('champ-mise').value, 10);
    const r = Game.placerMise(donnees, Number.isNaN(montant) ? 0 : montant);
    if (!r.ok) { $('message-erreur').textContent = r.erreur; return; }
    etatJeu.miseProchaine = montant;
    sauver(); majSolde();
  } else if (etatJeu.phase === 'VOL' && etatJeu.miseEnCours > 0) {
    encaisserMaintenant(performance.now());
  }
  majBoutonAction();
});

document.querySelectorAll('.mises-rapides button').forEach(b => {
  b.addEventListener('click', () => {
    $('champ-mise').value = b.dataset.mise === 'max' ? Math.max(Game.MISE_MIN, donnees.solde) : b.dataset.mise;
  });
});

function encaisserMaintenant(maintenant) {
  const m = Math.min(multCourant(maintenant), etatJeu.crashA);
  etatJeu.encaisseA = Math.floor(m * 100) / 100;
  const res = Game.resoudreManche(donnees, { mise: etatJeu.miseEnCours, crash: etatJeu.crashA, encaisseA: etatJeu.encaisseA });
  etatJeu.miseEnCours = 0;
  sauver(); majSolde(); majBoutonAction();
  $('affiche-mult').classList.add('gagne');
  $('message-vol').textContent = 'Encaissé : +' + res.gain + ' 🪙';
}

function demarrerAttente(maintenant) {
  etatJeu.phase = 'ATTENTE';
  etatJeu.finAttente = maintenant + DUREE_ATTENTE * 1000;
  etatJeu.miseProchaine = 0;
  etatJeu.encaisseA = null;
  $('affiche-mult').classList.remove('crash', 'gagne');
  majBandeCrashs(); majBoutonAction();
}

function demarrerVol(maintenant) {
  etatJeu.phase = 'VOL';
  etatJeu.debutVol = maintenant;
  etatJeu.crashA = Game.tirerCrash();
  etatJeu.miseEnCours = etatJeu.miseProchaine;
  etatJeu.miseProchaine = 0;
  majBoutonAction();
}

function terminerVol(maintenant) {
  etatJeu.phase = 'CRASH';
  etatJeu.finCrash = maintenant + DUREE_APRES_CRASH * 1000;
  Game.enregistrerCrash(donnees, etatJeu.crashA);
  if (etatJeu.miseEnCours > 0) {
    Game.resoudreManche(donnees, { mise: etatJeu.miseEnCours, crash: etatJeu.crashA, encaisseA: null });
    etatJeu.miseEnCours = 0;
    $('message-vol').textContent = 'Crashé… mise perdue 💥';
  } else if (etatJeu.encaisseA === null) {
    $('message-vol').textContent = 'Crashé à x' + etatJeu.crashA.toFixed(2);
  }
  sauver(); majSolde(); majBoutonAction();
}

function dessinerVol(maintenant) {
  const dpr = window.devicePixelRatio || 1;
  const larg = canvasVol.clientWidth, haut = canvasVol.clientHeight;
  if (canvasVol.width !== larg * dpr) { canvasVol.width = larg * dpr; canvasVol.height = haut * dpr; }
  ctxVol.setTransform(dpr, 0, 0, dpr, 0, 0);
  ctxVol.clearRect(0, 0, larg, haut);

  if (etatJeu.phase === 'ATTENTE') return;

  const tFin = etatJeu.phase === 'VOL' ? maintenant : etatJeu.debutVol + Math.log(etatJeu.crashA) / K_CROISSANCE * 1000;
  const duree = Math.max(0.001, (tFin - etatJeu.debutVol) / 1000);
  const mMax = Math.max(2, Math.exp(K_CROISSANCE * duree) * 1.15);
  const margeG = 10, margeB = 14;
  const xDe = (t) => margeG + (t / duree) * (larg * 0.82 - margeG);
  const yDe = (m) => haut - margeB - ((m - 1) / (mMax - 1)) * (haut * 0.8);

  ctxVol.beginPath();
  ctxVol.moveTo(xDe(0), yDe(1));
  const pas = duree / 60;
  for (let t = pas; t <= duree; t += pas) ctxVol.lineTo(xDe(t), yDe(Math.exp(K_CROISSANCE * t)));
  const xBout = xDe(duree), yBout = yDe(Math.exp(K_CROISSANCE * duree));
  ctxVol.lineTo(xBout, yBout);
  ctxVol.strokeStyle = etatJeu.phase === 'CRASH' ? '#E74C3C' : '#E94560';
  ctxVol.lineWidth = 3;
  ctxVol.stroke();
  ctxVol.lineTo(xBout, haut - margeB);
  ctxVol.lineTo(margeG, haut - margeB);
  ctxVol.closePath();
  ctxVol.fillStyle = 'rgba(233, 69, 96, 0.12)';
  ctxVol.fill();

  if (etatJeu.phase === 'VOL' && imageAvion.complete && imageAvion.naturalWidth > 0) {
    const lAvion = Math.min(90, larg * 0.22);
    const hAvion = lAvion * (imageAvion.naturalHeight / imageAvion.naturalWidth);
    const angle = -Math.atan2(yDe(1) - yBout, xBout - xDe(0)) * 0.6;
    ctxVol.save();
    ctxVol.translate(xBout, yBout);
    ctxVol.rotate(angle);
    ctxVol.drawImage(imageAvion, -lAvion * 0.3, -hAvion, lAvion, hAvion);
    ctxVol.restore();
  }
  if (etatJeu.phase === 'CRASH') {
    ctxVol.font = '40px sans-serif';
    ctxVol.fillText('💥', xBout - 20, yBout + 10);
  }
}

function boucleJeu(maintenant) {
  if (etatJeu.phase === 'ATTENTE') {
    const reste = Math.max(0, (etatJeu.finAttente - maintenant) / 1000);
    $('compte-rebours').textContent = Math.ceil(reste);
    $('message-vol').textContent = '';
    $('message-vol').innerHTML = 'Prochain vol dans <span id="compte-rebours">' + Math.ceil(reste) + '</span> s';
    $('affiche-mult').textContent = 'x1.00';
    if (maintenant >= etatJeu.finAttente) demarrerVol(maintenant);
  } else if (etatJeu.phase === 'VOL') {
    const m = multCourant(maintenant);
    if (m >= etatJeu.crashA) {
      terminerVol(maintenant);
    } else {
      $('affiche-mult').textContent = 'x' + m.toFixed(2);
      if (etatJeu.miseEnCours > 0) $('btn-action').textContent = 'ENCAISSER ' + Math.round(etatJeu.miseEnCours * m) + ' 🪙';
      const auto = parseFloat($('champ-auto').value);
      if (etatJeu.miseEnCours > 0 && !Number.isNaN(auto) && auto > 1 && m >= auto) encaisserMaintenant(maintenant);
      if (etatJeu.encaisseA === null) $('message-vol').textContent = '';
    }
  } else if (etatJeu.phase === 'CRASH') {
    $('affiche-mult').textContent = 'x' + etatJeu.crashA.toFixed(2);
    $('affiche-mult').classList.add('crash');
    if (maintenant >= etatJeu.finCrash) demarrerAttente(maintenant);
  }
  dessinerVol(maintenant);
  requestAnimationFrame(boucleJeu);
}
demarrerAttente(performance.now());
requestAnimationFrame(boucleJeu);
```

- [ ] **Step 2: Vérifier la cohérence des tests Node**

Run: `cd /Users/elliot/aviator && node tests/game.test.js`
Expected: toujours `OK` (app.js n'est pas chargé sous Node).

- [ ] **Step 3: Test manuel du jeu**

```bash
cd /Users/elliot/aviator && open index.html
```

Vérifier : compte à rebours 5 s → l'avion sélectionné décolle le long de la courbe →
multiplicateur monte → crash (💥, x affiché en rouge, puce ajoutée à la bande).
Miser 50 pendant l'attente → solde baisse tout de suite → ENCAISSER affiche le gain
en direct → encaisser crédite le gain (vert). Laisser crasher → « mise perdue ».
Auto-encaisser à 1.5 → encaisse seul vers x1.50. Bouton ← Salon grisé pendant un vol
avec mise, actif sinon. MAX met tout le solde.

- [ ] **Step 4: Commit**

```bash
cd /Users/elliot/aviator && git add app.js && git commit -m "feat: écran de jeu complet (vol animé, mises, encaissement, auto-cashout)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: Écran stats — courbe du capital + sélecteur de durée

**Files:**
- Modify: `app.js` (remplacer le stub `function dessinerStats() {}`)

**Interfaces:**
- Consumes: `donnees.capitalHistorique`, `donnees.stats`, `Game.SOLDE_DEPART`, `$()`.
- Produces: `dessinerStats()` appelée par `afficherEcran('stats')` (déjà branchée en Task 3).

- [ ] **Step 1: Remplacer le stub `dessinerStats`**

Supprimer la ligne `function dessinerStats() {} // remplacé en Task 5` et coller :

```js
// --- Écran stats ---
let dureeStats = 'tout';
document.querySelectorAll('#selecteur-duree button').forEach(b => {
  b.addEventListener('click', () => {
    dureeStats = b.dataset.duree;
    document.querySelectorAll('#selecteur-duree button').forEach(x => x.classList.toggle('actif', x === b));
    dessinerStats();
  });
});

function dessinerStats() {
  const canvas = $('canvas-stats');
  const ctx = canvas.getContext('2d');
  const dpr = window.devicePixelRatio || 1;
  const larg = canvas.clientWidth, haut = canvas.clientHeight;
  canvas.width = larg * dpr; canvas.height = haut * dpr;
  ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
  ctx.clearRect(0, 0, larg, haut);

  // série : capital de départ + solde après chaque manche jouée
  const tout = [Game.SOLDE_DEPART, ...donnees.capitalHistorique];
  const n = dureeStats === 'tout' ? tout.length : Math.min(tout.length, parseInt(dureeStats, 10) + 1);
  const serie = tout.slice(-n);

  const s = donnees.stats;
  $('resume-stats').innerHTML =
    carte(s.nbParties, 'parties jouées') + carte(s.totalMise, 'total misé 🪙') +
    carte(s.totalGagne, 'total gagné 🪙') + carte(s.plusGrosGain, 'plus gros gain 🪙');

  if (serie.length < 2) {
    ctx.fillStyle = '#9BA4B4';
    ctx.font = '15px -apple-system, sans-serif';
    ctx.textAlign = 'center';
    ctx.fillText('Joue une partie pour voir ta courbe 📉📈', larg / 2, haut / 2);
    return;
  }

  const mini = Math.min(...serie), maxi = Math.max(...serie);
  const ampli = Math.max(1, maxi - mini);
  const mg = 44, md = 14, mh = 16, mb = 26;
  const x = (i) => mg + (i / (serie.length - 1)) * (larg - mg - md);
  const y = (v) => mh + (1 - (v - mini) / ampli) * (haut - mh - mb);

  // grille : min, milieu, max
  ctx.strokeStyle = 'rgba(155,164,180,0.18)';
  ctx.fillStyle = '#9BA4B4';
  ctx.font = '11px -apple-system, sans-serif';
  ctx.textAlign = 'right';
  ctx.lineWidth = 1;
  for (const v of [mini, (mini + maxi) / 2, maxi]) {
    ctx.beginPath(); ctx.moveTo(mg, y(v)); ctx.lineTo(larg - md, y(v)); ctx.stroke();
    ctx.fillText(Math.round(v), mg - 6, y(v) + 4);
  }

  // ligne de départ (1000) si visible
  if (Game.SOLDE_DEPART >= mini && Game.SOLDE_DEPART <= maxi) {
    ctx.strokeStyle = 'rgba(241,196,15,0.35)';
    ctx.setLineDash([4, 4]);
    ctx.beginPath(); ctx.moveTo(mg, y(Game.SOLDE_DEPART)); ctx.lineTo(larg - md, y(Game.SOLDE_DEPART)); ctx.stroke();
    ctx.setLineDash([]);
  }

  // aire + courbe, couleur selon tendance
  const gagne = serie[serie.length - 1] >= serie[0];
  const couleur = gagne ? '#2ECC71' : '#E74C3C';
  ctx.beginPath();
  serie.forEach((v, i) => i === 0 ? ctx.moveTo(x(i), y(v)) : ctx.lineTo(x(i), y(v)));
  ctx.strokeStyle = couleur; ctx.lineWidth = 2.5; ctx.lineJoin = 'round'; ctx.stroke();
  ctx.lineTo(x(serie.length - 1), haut - mb); ctx.lineTo(x(0), haut - mb); ctx.closePath();
  ctx.fillStyle = gagne ? 'rgba(46,204,113,0.12)' : 'rgba(231,76,60,0.12)';
  ctx.fill();

  // point final + valeur
  const dx = x(serie.length - 1), dy = y(serie[serie.length - 1]);
  ctx.beginPath(); ctx.arc(dx, dy, 4, 0, Math.PI * 2); ctx.fillStyle = couleur; ctx.fill();
  ctx.textAlign = 'center'; ctx.fillStyle = '#EAEAEA'; ctx.font = 'bold 13px -apple-system, sans-serif';
  ctx.fillText(serie[serie.length - 1] + ' 🪙', Math.min(dx, larg - 40), Math.max(14, dy - 10));

  // libellé axe X
  ctx.fillStyle = '#9BA4B4'; ctx.font = '11px -apple-system, sans-serif';
  ctx.fillText((serie.length - 1) + ' dernière(s) partie(s)', larg / 2, haut - 8);
}

function carte(valeur, libelle) {
  return '<div class="carte-stat"><div class="valeur">' + valeur + '</div><div class="libelle">' + libelle + '</div></div>';
}
```

- [ ] **Step 2: Test manuel des stats**

```bash
cd /Users/elliot/aviator && open index.html
```

Jouer 3-4 manches (gains et pertes), ouvrir 📈 Mes stats. Vérifier : la courbe part de
1000 et suit les soldes, couleur verte si au-dessus du point de départ de la fenêtre,
rouge sinon, ligne pointillée dorée à 1000, boutons 10/25/50/Tout changent la fenêtre
(avec < 10 parties, les 4 boutons montrent la même chose — normal), les 4 cartes de
stats affichent des valeurs cohérentes. Sans aucune partie : message « Joue une partie… ».

- [ ] **Step 3: Commit**

```bash
cd /Users/elliot/aviator && git add app.js && git commit -m "feat: courbe d'évolution du capital avec sélecteur 10/25/50/tout

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6: PWA — manifest + service worker

**Files:**
- Create: `manifest.json`
- Create: `sw.js`
- Modify: `app.js` (enregistrement du service worker, 3 lignes à la fin)

**Interfaces:**
- Consumes: `img/icon-192.png`, `img/icon-512.png` (Task 2).
- Produces: app installable et jouable hors ligne.

- [ ] **Step 1: Écrire `manifest.json`**

```json
{
  "name": "Aviator — argent fictif",
  "short_name": "Aviator",
  "start_url": ".",
  "display": "standalone",
  "background_color": "#1A1A2E",
  "theme_color": "#16213E",
  "icons": [
    { "src": "img/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "img/icon-512.png", "sizes": "512x512", "type": "image/png" }
  ]
}
```

- [ ] **Step 2: Écrire `sw.js`**

```js
const CACHE = 'aviator-v1';
const FICHIERS = [
  '.', 'index.html', 'style.css', 'app.js', 'game.js', 'manifest.json',
  'img/lufthansa.png', 'img/emirates.png', 'img/icon-192.png', 'img/icon-512.png',
];
self.addEventListener('install', (e) => {
  e.waitUntil(caches.open(CACHE).then((c) => c.addAll(FICHIERS)).then(() => self.skipWaiting()));
});
self.addEventListener('activate', (e) => {
  e.waitUntil(
    caches.keys().then((cles) => Promise.all(cles.filter((k) => k !== CACHE).map((k) => caches.delete(k))))
      .then(() => self.clients.claim())
  );
});
self.addEventListener('fetch', (e) => {
  e.respondWith(caches.match(e.request).then((r) => r || fetch(e.request)));
});
```

- [ ] **Step 3: Enregistrer le service worker à la fin d'`app.js`**

```js
if ('serviceWorker' in navigator && location.protocol === 'https:') {
  navigator.serviceWorker.register('sw.js');
}
```

(Condition `https:` : évite les erreurs en test local via `file://`.)

- [ ] **Step 4: Test rapide**

Run: `cd /Users/elliot/aviator && node tests/game.test.js && open index.html`
Expected: tests OK, la page se charge sans erreur console malgré le manifest.

- [ ] **Step 5: Commit**

```bash
cd /Users/elliot/aviator && git add manifest.json sw.js app.js && git commit -m "feat: PWA installable (manifest + service worker hors-ligne)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 7: Déploiement GitHub Pages

**Files:** aucun nouveau fichier (repo + push).

**Interfaces:**
- Consumes: repo local complet, `gh` CLI authentifié (compte ewzix1234, déjà utilisé pour musculog/restaurant-pwa).
- Produces: `https://ewzix1234.github.io/aviator/` en ligne.

- [ ] **Step 1: Créer le repo GitHub et pousser**

```bash
cd /Users/elliot/aviator
gh repo create ewzix1234/aviator --public --source=. --remote=origin --push
```

Expected: repo créé, branche main poussée.

- [ ] **Step 2: Activer GitHub Pages sur main**

```bash
gh api repos/ewzix1234/aviator/pages -X POST -f "source[branch]=main" -f "source[path]=/" 2>/dev/null \
  || gh api repos/ewzix1234/aviator/pages -X PUT -f "source[branch]=main" -f "source[path]=/"
```

Expected: réponse JSON avec `"status"`. Le site est disponible après 1-2 min.

- [ ] **Step 3: Vérifier le déploiement**

```bash
sleep 90 && curl -s -o /dev/null -w "%{http_code}" https://ewzix1234.github.io/aviator/
```

Expected: `200`. Sinon réessayer après 1 min (build Pages parfois lent).

- [ ] **Step 4: Test final sur l'URL publique**

Ouvrir `https://ewzix1234.github.io/aviator/` : jouer une manche complète, vérifier
salon/carrousel/jeu/stats, et que le service worker s'enregistre (https cette fois).

---

## Self-Review (fait à l'écriture du plan)

- **Couverture spec :** salon+carrousel (T3), jeu+auto-cashout+bande crashs+retour salon désactivé (T4), stats+courbe+sélecteur 10/25/50/tout (T5), avions détourés (T2), PWA (T6), déploiement (T7), logique+distribution+recharge (T1+T3). ✔
- **Placeholders :** le seul stub (`dessinerStats`) est explicitement déclaré en T3 et remplacé en T5. ✔
- **Cohérence des types :** `Game.*` signatures identiques entre T1 (définition) et T3-T5 (usage) ; IDs DOM de T3 utilisés tels quels en T4-T5. ✔
